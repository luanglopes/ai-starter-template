# API Template

Cloudflare Workers API starter with:
- Better Auth (email/password + OTP + reset)
- Billing endpoints (checkout, portal, plan)
- Todo CRUD endpoints
- AI description enhancement for todos (Gemini/Ollama provider abstraction)

## Development

```bash
bun install
bun run dev
```

Server runs on `http://localhost:8787`.
